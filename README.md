# Carver

A browser-first pixel puzzle where every move carves the world into ice.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
