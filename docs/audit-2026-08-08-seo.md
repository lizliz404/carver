# Carver SEO / GSC / GA4 / GEO audit — 2026-08-08

Live: https://carver.lizliz.xyz  
Stack: Next.js App Router, `output: 'export'`  
Source: `app/layout.tsx`, `app/page.tsx`, `app/robots.ts`, `app/sitemap.ts`, `components/CarverPage.tsx`, `public/`  
Live evidence: `curl`/HTTPS fetch for `/`, `/robots.txt`, `/sitemap.xml`, `/og-image.png`, `/llms.txt` (2026-08-08)

Canonical host (bindings / live custom domain): **carver.lizliz.xyz** — prior `carver.pages.dev` residue confirmed cleared on live.

## Checklist

| Item | Status | Action | File:line |
|---|---|---|---|
| robots.txt allows pages + Sitemap | Pass | Live: CF managed block + app `Allow: /`; `Sitemap: https://carver.lizliz.xyz/sitemap.xml`. | `app/robots.ts:7-14` |
| sitemap.xml valid, real `<loc>` | Pass | Live 200 `application/xml`; `<loc>` count **1** → `https://carver.lizliz.xyz` (no pages.dev). | `app/sitemap.ts:5-14` |
| Canonical single, correct host | Pass | Live single canonical `https://carver.lizliz.xyz`; og:url same. No pages.dev in live HTML. | `app/layout.tsx:6,12,36-38` |
| http→https | Pass | `http://` → 301 → `https://carver.lizliz.xyz/` | — |
| Redirects / `_redirects` splat | Pass | No `public/_redirects`; no loop observed. | — |
| noindex leak | Pass | Live `robots: index, follow`; no `x-robots-tag` noindex. | `app/layout.tsx:67-77` |
| GA4 G-TXVLTJJ878 | Pass | Live count **3**; preload + gtag.js + config. ID untouched. | `app/layout.tsx:89-98` |
| GA4 placement | Pass | Next `Script` `afterInteractive` (house pattern); single-route shell. | `app/layout.tsx:90-99` |
| GSC verification token | n/a | Covered by **lizliz.xyz domain property** — subdomain token not required. Not fabricated. | — |
| GEO entity signals | Pass | Visible product/category/mechanics + footer `by Liz` → lizliz.xyz. | `components/CarverPage.tsx:337-347` |
| JSON-LD crawlable + truthful | Fixed | Live had VideoGame only inside RSC/`next/script` payload (**0** `<script type="application/ld+json">` tags). Switched to plain `<script>` for static HTML emit. Absolute URLs; free Offer price `0` truthful; no fake FAQPage. | `app/page.tsx:39-44` |
| Factual copy | Pass | Dirt→Ice→Void brace mechanics specific. | `components/CarverPage.tsx` help + tagline |
| FAQ / How-it-works | Pass (partial) | How-to-play popover + h2; no separate FAQPage (would invent Q/A). | `components/CarverPage.tsx:213-264` |
| llms.txt | Fixed | Live `/llms.txt` was soft-404 HTML. Added root `public/llms.txt`. | `public/llms.txt` |
| AI crawler access | Flag | CF managed robots **Disallow** GPTBot + ClaudeBot (+ others). Code cannot fix; operator decision. | live `/robots.txt` |
| Title ~60 Primary — Product | Pass | `Carver — Damage Becomes Infrastructure` (38). | `app/layout.tsx:18-20` |
| Meta description 120–160 pain-first | Pass | 141 chars; mechanics-first. | `app/layout.tsx:8-9` |
| OG + Twitter | Pass | title/desc/url/image 1200×630 + `summary_large_image`; host correct. | `app/layout.tsx:38-58`; `public/og-image.png` |
| One H1 + hierarchy | Pass | Brand `<h1>`; help `<h2>`. | `components/CarverPage.tsx:188-190,222-227` |
| favicon + apple-touch + theme-color | Fixed | Icons present. `metadata.themeColor` did **not** emit live (`theme-color` absent in `<head>`). Moved to `export const viewport`. | `app/layout.tsx:11-14`; `public/*` |
| hreflang / lang | Pass | `lang="en"`; client toggles `zh-CN`. No fake static hreflang routes. | `app/layout.tsx:86` |
| Structured data validity | Pass (after LD fix) | Single VideoGame; absolute https; no duplicate `@id`. | `app/page.tsx` |
| CWV red flags | Pass (spot) | og-image 1200×630; icon dims declared; canvas game (INP depends on input — not SEO-blocked). | — |

## Live evidence (this pass)

- `GET /` → 200; GA4 `G-TXVLTJJ878` ×3; canonical/og host **carver.lizliz.xyz**; **no** `pages.dev` in HTML
- `GET /robots.txt` → Allow `/` + Sitemap on lizliz; CF managed block Disallows GPTBot/ClaudeBot
- `GET /sitemap.xml` → 1× `<loc>https://carver.lizliz.xyz</loc>`
- `GET /og-image.png` → 200, 1200×630
- `GET /llms.txt` → **404** (pre-fix; fixed in source)
- `theme-color` meta → **absent** live (pre-fix; fixed via viewport)
- JSON-LD script tags in initial HTML → **0** (pre-fix; fixed off `next/script`)

## Changes this pass

1. `app/layout.tsx` — `themeColor` → `viewport.themeColor` so static HTML emits `<meta name="theme-color">`
2. `app/page.tsx` — VideoGame JSON-LD via plain `<script type="application/ld+json">` (crawlable initial HTML)
3. `public/llms.txt` — GEO citability stub
4. `docs/audit-2026-08-08-seo.md` — this report

## Residual

### P0
- None in source after this pass. **Deploy required** (Hermes commit/push) for theme-color, crawlable JSON-LD, and `/llms.txt` to go live.

### P1
- Cloudflare managed robots block GPTBot/ClaudeBot — flag for operator if GEO citation crawl desired; not fixable from repo robots alone.
- Confirm build env never sets `NEXT_PUBLIC_SITE_URL` back to `carver.pages.dev` (live currently correct; no `.env` in repo).

### P2
- Optional Organization/WebSite `@graph` beside VideoGame.
- Optional visible FAQ + matching FAQPage later (do not invent).
- `icon.png` remains 120×120 (sizes claim matches); optional true 512 later.
- Stale local `out/` (gitignored) still shows old pages.dev — ignore; regenerate only on deploy build.

## Verification

- Did **not** run `npm run build`: no build-config change; static export emit verified by Next viewport + plain-script pattern. Hermes Preview build is the gate.
- `grep -c G-TXVLTJJ878 app/layout.tsx` → **3** intact.
- No commit, no push (per MASTER).
