# Carver SEO / GSC / GA4 / GEO audit — 2026-08-07

Live: https://carver.lizliz.xyz  
Stack: Next.js 15 App Router, `output: 'export'`  
Source audited: `app/layout.tsx`, `app/page.tsx`, `app/robots.ts`, `app/sitemap.ts`, `components/CarverPage.tsx`, `public/`  
Live evidence: `curl` HEAD/body for `/`, `/robots.txt`, `/sitemap.xml`, `/og-image.png` (2026-08-07)

## Checklist

| Item | Status | Action taken | File:line |
|---|---|---|---|
| sitemap.xml exists, valid, real URLs | Fixed | Fallback site URL was `https://carver.pages.dev` (live sitemap loc + robots Sitemap pointed there). Changed default to `https://carver.lizliz.xyz`. | `app/sitemap.ts:5`, `app/robots.ts:5` |
| robots.txt allows pages + references sitemap | Pass (after URL fix) | Already `Allow: /` + sitemap ref; URL domain corrected with siteUrl fallback. | `app/robots.ts:7-14` |
| GSC verification token | Missing | Not present in source or live HTML. Reported only — did not invent a token. | — |
| Canonical single, correct host | Fixed | Live canonical/og:url were `https://carver.pages.dev`. Fallback → `carver.lizliz.xyz`; `metadataBase` + `alternates.canonical` unchanged shape. | `app/layout.tsx:6,11,33-35` |
| noindex not leaking on indexable pages | Pass | `robots: { index: true, follow: true }` + live `index, follow`. | `app/layout.tsx:63-74` |
| GA4 G-TXVLTJJ878 present | Pass | Intact; `gtag.js` + config in layout. Live preload/script confirm. | `app/layout.tsx:85-95` |
| GA4 ID not changed | Pass | Left `G-TXVLTJJ878` untouched. | `app/layout.tsx:87,94` |
| GA4 placement (head/async; all routes) | Pass | Next `Script` `afterInteractive` (house pattern). Single-route shell — loads on `/`. | `app/layout.tsx:86-95` |
| GEO entity signals (what / category / who) | Fixed | Product + puzzle category already in tagline/help. Added visible maker credit `by Liz` → lizliz.xyz in footer. | `components/CarverPage.tsx:337-347` |
| JSON-LD truthful + valid | Fixed | Kept `VideoGame`; added `image`, `inLanguage`, `author` (Liz), `isPartOf` (lizliz.xyz). Absolute URL via corrected siteUrl. No invented FAQPage (no visible Q/A pairs). | `app/page.tsx:4-35` |
| Specific factual copy | Pass | Mechanics already specific (dirt→ice, void braces, slide). Help popover rules retained. | `components/CarverPage.tsx` copy + help |
| FAQ / How-it-works / About | Pass (partial) | How-to-play popover present. No separate About/FAQ section — game shell; not added as marketing chapter. | `components/CarverPage.tsx:213-264` |
| `<title>` unique ~60 | Pass | `Carver — Damage Becomes Infrastructure` (38 chars). | `app/layout.tsx:13-15` |
| meta description 120–160, pain-first | Fixed | Was 101 chars. Expanded to pain-first mechanics copy (141 chars). | `app/layout.tsx:8-9` |
| OG + Twitter cards | Pass (after URL fix) | title/description/type/url/image 1200×630 + `summary_large_image` already present; host corrected via metadataBase. | `app/layout.tsx:36-55`; `public/og-image.png` 1200×630 |
| hreflang / lang | Pass (known) | `html lang="en"`; client locale toggles `documentElement.lang` (en / zh-CN). No `/zh/` routes — static-host 404 on fresh `/zh/` known & acceptable. | `app/layout.tsx:82`; `components/CarverPage.tsx:135-137` |
| one h1 + heading hierarchy | Fixed | Brand was a `<span>`; promoted to `<h1>`. Help uses `<h2>`. | `components/CarverPage.tsx:188-190,222-227` |
| favicon + apple-touch + theme-color | Fixed | Favicon/apple present. Added `themeColor: "#05070a"`. Corrected `icon.png` sizes claim `512x512` → `120x120` (actual file). | `app/layout.tsx:56-62`; `public/*` |
| JSON-LD syntax / absolute URLs | Pass (after URL fix) | Single VideoGame block; absolute urls via siteUrl. | `app/page.tsx` |

## Live evidence (pre-fix)

- `GET /` → HTTP 200; no `x-robots-tag` noindex
- GA4 `G-TXVLTJJ878` present
- Canonical / OG / Twitter / JSON-LD URL host: **carver.pages.dev** (wrong vs live custom domain)
- `robots.txt`: `Allow: /`; `Sitemap: https://carver.pages.dev/sitemap.xml`
- `sitemap.xml`: `<loc>https://carver.pages.dev</loc>`
- `og-image.png`: 200, 1200×630
- No `theme-color`, no `google-site-verification`

## Residual

### P0
- None remaining in source after this pass. **Deploy required** (Hermes commit/push) before live URLs flip off `pages.dev`.

### P1
- GSC property verification for `carver.lizliz.xyz` (meta or DNS TXT) — token not in repo; owner must add after GSC setup.
- Confirm Cloudflare Pages / Workers Builds does **not** override `NEXT_PUBLIC_SITE_URL` back to `pages.dev` at build time (no `.env` in repo; fallback now correct).

### P2
- Optional dedicated Organization / WebSite `@graph` nodes (agent-crm style) — VideoGame + author/isPartOf is enough for a single-page game toy.
- Optional FAQPage only if visible FAQ Q/A is added later (do not invent schema without visible pairs).
- `icon.png` is 120×120 (not 512); fine for favicon link — optional true 512 app icon later.
- zh strings for footer maker credit (footer was already EN-hardcoded).

## Verification

- Did **not** run `npm run build` / `check`: no changes to `index.html` or build config (`next.config.ts` untouched). TS metadata/UI-only edits.
- No commit, no push (per MASTER).
